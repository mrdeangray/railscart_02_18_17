<%= form_for(@gritty2) do |f| %>
  <% if @gritty2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gritty2.errors.count, "error") %> prohibited this gritty2 from being saved:</h2>

      <ul>
      <% @gritty2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.text_field :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
