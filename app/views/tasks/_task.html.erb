<p>
    <%if task.completed_at.nil?%>
<%=task.content%>
<%=link_to "Delete Task", project_task_path(@project, task.id), method: :delete, data: {confirm: "Estas seguro?"} %>
<%=link_to "Mark As Complete", mark_as_complete_project_task_path(@project, task.id), method: :patch%>

<%else%>
<p> 
<div class='completed_task'><%=task.content%></div>
<%=link_to "Delete Task", project_task_path(@project, task.id), method: :delete, data: {confirm: "Estas seguro?"} %>
<%=link_to "Mark As Incomplete", mark_as_incomplete_project_task_path(@project, task.id), method: :patch%>
</p>
<%end%>

</p>